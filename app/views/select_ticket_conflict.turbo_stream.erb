<%= turbo_stream.update "notices" do %>
  <div class="border-2 border-red-400 py-2 px-2 bg-red-100">
    Sorry! This seat has been claimed by another patron.
  </div>
<% end %>

<%= turbo_stream.replace "#{dom_id(@ticket)}_link" do %>
  <%= render "unavailable_ticket", ticket: @ticket %>
<% end %>
