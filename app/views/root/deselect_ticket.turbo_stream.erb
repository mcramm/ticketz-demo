<%= turbo_stream.replace "#{dom_id(@ticket)}_link" do %>
  <%= render "select_ticket_link", ticket: @ticket %>
<% end %>

<%= turbo_stream.update "selected-tickets" do %>
  <%= render "selected_tickets", tickets: @selected_tickets %>
<% end %>
