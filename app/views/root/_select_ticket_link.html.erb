<%= link_to(url_for(action: "select_ticket", id: ticket.id), { data: { "turbo-method" => "post" }, id: "#{dom_id(ticket)}_link" }) do %>
  <%= render "available_ticket", allow_hover: true %>
<% end %>
