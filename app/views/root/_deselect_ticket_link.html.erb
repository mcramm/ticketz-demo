<%= link_to(url_for(action: "deselect_ticket", id: ticket.id), { data: { "turbo-method" => "post" }, id: "#{dom_id(ticket)}_link" }) do %>
  <%= render "selected_ticket", allow_hover: true %>
<% end %>
